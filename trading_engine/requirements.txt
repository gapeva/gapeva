# 1. Core Math (Pinned to prevent conflicts with Numpy 2.0)
numpy<2.0.0

# 2. Dataframes (Force binary version to avoid compilation errors)
pandas==2.2.3

# 3. Technical Analysis (Install from ZIP URL to bypass Git errors)
https://github.com/twopirllc/pandas-ta/archive/refs/heads/development.zip

# 4. Trading & Database
ccxt
python-dotenv
SQLAlchemy
psycopg2-binary
