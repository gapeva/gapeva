# 1. Core Math (Pinned to prevent conflicts)
numpy<2.0.0

# 2. Dataframes (Force specific binary version to avoid compiling)
pandas==2.2.3

# 3. Technical Analysis (Install from Git to get Python 3.12 support)
git+https://github.com/twopirllc/pandas-ta.git@development

# 4. Trading & Database
ccxt
python-dotenv
SQLAlchemy
psycopg2-binary
