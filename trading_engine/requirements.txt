# 1. Core Math (Pinned to 1.x to prevent financial library crashes)
numpy<2.0.0

# 2. Dataframes (Pinned to Binary version to prevent compile errors)
pandas==2.2.3

# 3. Technical Analysis (Install Stable Version directly from Zip)
# This bypasses PyPI dependency hell and git authentication errors
https://github.com/twopirllc/pandas-ta/archive/refs/tags/0.3.14b.zip

# 4. Trading & Infrastructure
ccxt
python-dotenv
SQLAlchemy
psycopg2-binary



